version: '2.3'
services:
  wasp-backend:
    build: .
  wasp-frontend:
    image: nginx:1.13-alpine
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - wasp-backend
    ports:
      - 5001:80